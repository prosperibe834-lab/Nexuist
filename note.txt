https://github.com/prosperibe834-lab/Nexuist.git


////////////////////////////////////////////////////////////
// UNIVERSAL SCROLL ANIMATION ENGINE
// Paste this ONCE at the very bottom of your JS file
////////////////////////////////////////////////////////////

document.addEventListener("DOMContentLoaded", () => {

    // All animation selectors
    const animatedElements = document.querySelectorAll(`
        .st-animate,
        .at-animate,
        .nx-animate,
        .sp-animate,
        .dm-animate,
        .cs-animate,
        .ts-animate,
        .ln-animate
    `);

    // Reusable observer
    const globalObserver = new IntersectionObserver((entries, observer) => {

        entries.forEach(entry => {

            if (entry.isIntersecting) {

                // Activate animation
                entry.target.classList.add("active");

                ////////////////////////////////////////////////////////////
                // NX COUNTERS
                ////////////////////////////////////////////////////////////
                if (
                    entry.target.classList.contains('nx-stats-bar') &&
                    !entry.target.classList.contains('counted')
                ) {

                    entry.target.classList.add('counted');

                    const counters = document.querySelectorAll('.nx-counter');

                    counters.forEach(counter => {

                        const target = +counter.getAttribute('data-target');
                        const increment = target / 50;

                        const updateCount = () => {

                            const current = +counter.innerText;

                            if (current < target) {

                                counter.innerText = Math.ceil(current + increment);

                                setTimeout(updateCount, 30);

                            } else {

                                counter.innerText = target;
                            }
                        };

                        updateCount();
                    });
                }

                // Stop observing after animation
                observer.unobserve(entry.target);
            }

        });

    }, {
        threshold: 0.12
    });

    // Observe all elements
    animatedElements.forEach((el, index) => {

        // Auto stagger delay
        el.style.transitionDelay = `${index * 0.05}s`;

        globalObserver.observe(el);
    });

});

////////////////////////////////////////////////////////////
// RESPONSIVE CANVAS CHART RESIZE FIX
////////////////////////////////////////////////////////////

window.addEventListener("resize", () => {

    const canvas = document.getElementById('at-live-chart');

    if (!canvas) return;

    const ctx = canvas.getContext('2d');

    canvas.width = canvas.parentElement.offsetWidth;
    canvas.height = 350;

    ctx.clearRect(0, 0, canvas.width, canvas.height);

    ctx.beginPath();
    ctx.strokeStyle = '#0070f3';
    ctx.lineWidth = 3;
    ctx.moveTo(0, 200);

    for (let i = 0; i < canvas.width; i += 20) {

        const y = 150 + Math.sin(i * 0.05) * 40 + Math.random() * 30;

        ctx.lineTo(i, y);
    }

    ctx.stroke();

    ctx.lineTo(canvas.width, canvas.height);
    ctx.lineTo(0, canvas.height);

    const grad = ctx.createLinearGradient(0, 0, 0, 400);

    grad.addColorStop(0, 'rgba(0, 112, 243, 0.2)');
    grad.addColorStop(1, 'transparent');

    ctx.fillStyle = grad;
    ctx.fill();
});

////////////////////////////////////////////////////////////
// CLOSE MOBILE MENU WHEN CLICKING OUTSIDE
////////////////////////////////////////////////////////////

document.addEventListener("click", (e) => {

    const navMenu = document.getElementById("nav-menu");
    const menuToggle = document.getElementById("mobile-menu");
    const navbar = document.getElementById("navbar");

    if (!navMenu || !menuToggle || !navbar) return;

    const clickedInsideMenu = navMenu.contains(e.target);
    const clickedToggle = menuToggle.contains(e.target);

    if (
        navMenu.classList.contains("is-open") &&
        !clickedInsideMenu &&
        !clickedToggle
    ) {

        navMenu.classList.remove("is-open");
        navbar.classList.remove("menu-is-active");
        menuToggle.classList.remove("is-active");

        document.body.style.overflow = "initial";
    }
});

////////////////////////////////////////////////////////////
// SMOOTH SCROLL FOR INTERNAL LINKS
////////////////////////////////////////////////////////////

document.querySelectorAll('a[href^="#"]').forEach(anchor => {

    anchor.addEventListener('click', function (e) {

        const target = document.querySelector(this.getAttribute('href'));

        if (target) {

            e.preventDefault();

            target.scrollIntoView({
                behavior: 'smooth',
                block: 'start'
            });
        }
    });
});

////////////////////////////////////////////////////////////
// GLOBAL HOVER GLOW EFFECT
////////////////////////////////////////////////////////////

document.querySelectorAll(`
    .io-card,
    .nx-card,
    .sp-step-card,
    .cs-card,
    .ts-card,
    .ln-card,
    .dm-benefit-tile
`).forEach(card => {

    card.addEventListener('mousemove', (e) => {

        const rect = card.getBoundingClientRect();

        const x = e.clientX - rect.left;
        const y = e.clientY - rect.top;

        card.style.setProperty('--mouse-x', `${x}px`);
        card.style.setProperty('--mouse-y', `${y}px`);
    });

});



git add .
git commit -m "i added admin auth"
git push



next time dont put html and js together send them seperate and why cant i download when i click that button both inspect screen and am not seing some stufss here well and why cant i approve both reject disposer both that  
Attested Document Proof Vault
am not seing the image why like many things are not working just rebuild it and send me the html css js code to paste at my min make it to be correct ohh and responsive dont make ny mistake 


/* ============================= */
/* RESPONSIVE HEADER + SIDEBAR */
/* ============================= */

@media (max-width: 992px) {

    .mobile-menu-btn{
        display: flex;
        align-items: center;
        justify-content: center;
    }

    .desktop-only{
        display: none !important;
    }

    .top-header{
        padding: 0 15px;
    }

    .header-left{
        gap: 12px;
    }

    .header-right{
        gap: 10px;
    }

    .logo-text p{
        display: none;
    }

    .logo-text h2{
        font-size: 15px;
    }

    .qt-dropdown-btn{
        padding: 10px 12px;
    }

    .qt-user-sub{
        display: none;
    }

    .qt-user-title{
        font-size: 12px;
    }

    /* SIDEBAR */

    .sidebar{
        position: fixed;
        top: var(--header-height);
        left: -100%;
        width: 280px;
        height: calc(100vh - var(--header-height));
        z-index: 2000;
        transition: 0.3s ease;
    }

    .sidebar.active{
        left: 0;
    }

    /* MAIN CONTENT */

    .investment-container{
        margin-left: 0 !important;
        width: 100% !important;
        padding: 20px 15px;
    }

    .main-layout{
        display: block;
    }

    /* HEADER SECTION */

    .stock-dashboard-header{
        flex-direction: column;
        align-items: flex-start;
        gap: 20px;
        padding: 24px 18px;
    }

    .header-content h1{
        font-size: 1.5rem;
        line-height: 1.3;
    }

    .header-content p{
        font-size: 14px;
    }

    .header-stats{
        width: 100%;
        flex-direction: row;
        gap: 12px;
    }

    .stat-bubble{
        flex: 1;
        width: 100%;
        padding: 18px;
    }

    .stat-bubble h3{
        font-size: 1.3rem;
    }

    /* INSIGHTS GRID */

    .insights-grid{
        grid-template-columns: 1fr;
    }

    .allocation-canvas{
        flex-direction: column;
        height: auto;
    }

    /* PLANS */

    .plans-super-grid{
        grid-template-columns: 1fr;
    }

    /* FEATURES */

    .features-wrapper{
        grid-template-columns: 1fr;
    }

    /* FOOTER STRIP */

    .sector-strip{
        flex-direction: column;
        align-items: flex-start;
    }

    .sector-icons{
        gap: 14px;
    }

}


/* ============================= */
/* MOBILE SMALL DEVICES */
/* ============================= */

@media (max-width: 576px){

    .top-header{
        height: 65px;
    }

    :root{
        --header-height: 65px;
    }

    .logo-img{
        width: 28px;
        height: 28px;
    }

    .logo-text h2{
        font-size: 14px;
    }

    .qt-dropdown-btn{
        padding: 8px 10px;
    }

    .qt-user-title{
        font-size: 11px;
    }

    .icon-btn{
        font-size: 18px;
    }

    .avatar{
        width: 30px;
        height: 30px;
        font-size: 12px;
    }

    .investment-container{
        padding: 15px 12px;
    }

    .stock-dashboard-header{
        padding: 20px 15px;
        border-radius: 14px;
    }

    .header-content h1{
        font-size: 1.3rem;
    }

    .tag-row{
        gap: 8px;
    }

    .tag{
        font-size: 12px;
        padding: 6px 10px;
    }

    .header-stats{
        flex-direction: column;
    }

    .stat-bubble{
        width: 100%;
    }

    .insights-card{
        padding: 18px;
    }

    .css-doughnut{
        width: 110px;
        height: 110px;
    }

    .doughnut-center{
        width: 78px;
        height: 78px;
    }

    .plan-card{
        border-radius: 14px;
    }

    .plan-header{
        padding: 18px;
    }

    .plan-body{
        padding: 18px;
    }

    .features-block{
        padding: 22px 18px;
    }

    .feature-card p{
        font-size: 13px;
    }

    .sector-icon{
        font-size: 13px;
    }

}


/* ============================= */
/* SIDEBAR OVERLAY */
/* ============================= */

.sidebar-overlay{
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.5);
    z-index: 1500;
    opacity: 0;
    visibility: hidden;
    transition: 0.3s ease;
}

.sidebar-overlay.active{
    opacity: 1;
    visibility: visible;
}


/* ===== MOBILE SIDEBAR TOGGLE FIX ===== */

@media (max-width: 992px) {

    .mobile-menu-btn{
        display: block;
    }

    .sidebar{
        position: fixed;
        top: var(--header-height);
        left: -100%;
        width: 280px;
        height: calc(100vh - var(--header-height));

        z-index: 9999;

        transition: 0.3s ease;
    }

    .sidebar.show{
        left: 0;
    }

    /* Optional overlay effect */
    body.sidebar-open{
        overflow: hidden;
    }

    .investment-container{
        margin-left: 0;
        width: 100%;
    }

}




























send me the html css js code to paste at my main /* =========================================

   NEXUIST FINTECH ROOT VARIABLES [cite: 1]

========================================= */

:root {

    /* ===== PRIMARY BRAND COLORS ===== */

    --primary-color: #6c63ff;

    --primary-hover: #7c73ff;

    --secondary-color: #00d4ff;

    --accent-color: #8b5cf6;



    /* ===== BACKGROUND COLORS ===== */

    --bg-main: #0b101e;

    --bg-secondary: #111827;

    --bg-card: #151b2b;

    --bg-sidebar: #0f1524;

    --bg-header: #070b14;

    --bg-hover: #1e293b;



    /* ===== GLASS EFFECTS ===== */

    --glass-bg: rgba(255, 255, 255, 0.03);

    --glass-border: rgba(255, 255, 255, 0.06);

    --glass-shadow: rgba(0, 0, 0, 0.5);



    /* ===== TEXT COLORS ===== */

    --text-primary: #ffffff;

    --text-secondary: #94a3b8;

    --text-muted: #64748b;



    /* ===== BORDER COLORS ===== */

    --border-color: rgba(255, 255, 255, 0.08);



    /* ===== GLOW EFFECTS ===== */

    --glow-primary: 0 0 15px rgba(108, 99, 255, 0.35);



    /* ===== DESIGN UTILITIES ===== */

    --radius-md: 14px;

    --transition-normal: 0.3s cubic-bezier(0.4, 0, 0.2, 1);

    --font-main: 'Inter', sans-serif;

    --z-sidebar: 1000;

}



/* =========================================

   LIGHT MODE OVERRIDES

========================================= */

html[data-theme="light"] {

    --bg-main: #f8fafc;

    --bg-secondary: #f1f5f9;

    --bg-card: #ffffff;

    --bg-sidebar: #ffffff;

    --bg-header: #ffffff;

    --bg-hover: #e2e8f0;



    --glass-bg: rgba(15, 23, 42, 0.03);

    --glass-border: rgba(15, 23, 42, 0.08);

    --glass-shadow: rgba(15, 23, 42, 0.05);



    --text-primary: #0f172a;

    --text-secondary: #475569;

    --text-muted: #94a3b8;

    --border-color: rgba(15, 23, 42, 0.08);

    --glow-primary: 0 4px 12px rgba(108, 99, 255, 0.15);

}



/* =========================================

   BASE RESET & LAYOUT STRUCTURING

========================================= */

* {

    margin: 0;

    padding: 0;

    box-sizing: border-box;

    font-family: var(--font-main);

}



body {

    background-color: var(--bg-main);

    color: var(--text-primary);

    overflow-x: hidden;

    transition: background-color var(--transition-normal);

}



git add .
git commit -m "i fixed notification"
git 

php artisan investments:process
php artisan schedule:run
php artisan serve

php artisan schedule:work



